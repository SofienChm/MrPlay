import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color background = Color(0xFF0F0F0F);
  static const Color surface = Color(0xFF1A1A1A);
  static const Color surfaceLight = Color(0xFF1E1E1E);
  static const Color surfaceLighter = Color(0xFF2A2A2A);
  static const Color border = Color(0xFF2C2C2C);

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textTertiary = Colors.white54;
  static const Color textMuted = Colors.white38;
  static const Color textDim = Colors.white24;
  static const Color textFaint = Colors.white12;
  static const Color textVeryFaint = Colors.white10;

  static const Color accent = Colors.redAccent;
  static const Color surfaceOverlay = Color(0xF01A1A1A);
}
